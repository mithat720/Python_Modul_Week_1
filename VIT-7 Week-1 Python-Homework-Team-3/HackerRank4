k = int(input())
student_marks = {}
for _ in range(k):
        name, *line = input().split()
        scores = list(map(float, line))
        student_marks[name] = scores
query_name = input()
Average=sum(student_marks[query_name])/k
print(f"{Average:.2f}")