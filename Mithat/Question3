#- Question 3: Write a Python code that receives a start and end value from the user and prints all the numbers between these values ​​(including the end value) on the screen.

n1=input("enter an integer number as a start ")
while not n1.isdigit():
    n1=input("Please enter an integer number as a start ")
n2=input("enter an integer number as an end number ")
while not n2.isdigit() or int(n2)<=int(n1):
    n2=input("Please enter an integer number as a end number ")
numbers=""
for i in range(int(n1),int(n2)+1):
    numbers=numbers+" "+str(i)
print("Numbers between",n1, "and",n2,numbers)

